<form method="post" action="{{ path('app_analytique_delete', {'id': analytique.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ analytique.id) }}">
    <button class="btn">Delete</button>
</form>
