<form method="post" action="{{ path('app_penalite_credit_delete', {'id': penalite_credit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ penalite_credit.id) }}">
    <button class="btn">Delete</button>
</form>
