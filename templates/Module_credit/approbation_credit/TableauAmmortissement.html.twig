{% extends 'base.html.twig' %}

{% block title %}Tableau d'amortissement{% endblock %}
{% block head %}Tableau d'amortissement {% endblock %}

{% block body %}
    {{ include('modal.html.twig') }}
    <div class="card p-3">
        {# {% for tableau in tableau %} #}
             <div class="card">
                <div class="row text-secondary m-3">
                    <div class="col-4 col-sm-4 col-sm-4">
                        <p class="fw-bold">Montant credit : {{ MontantDemande }}  </p>
                        <p class="fw-bold"> Taux annuelle : {{ InteretA }} % </p>
                    </div>
                    <div class="col-4 col-sm-4 col-sm-4">
                        <p class="fw-bold">Code credit : {{ codecredit }} </p>
                        <p class="fw-bold"> Nombre tranche : {{ Periode }} </p>
                    </div>
                </div>
            </div>
        {# {% endfor %} #}
        <table class="table table-bordered mt-2">
            <thead>
                <tr>
                    <th>Periode(N)</th>
                    <th>Date de remboursement</th>
                    <th>Capital </th>
                    <th>Interet </th>
                    <th>Remboursement </th>
                    <th>Capital restant du </th>
                    <th>Interet Restant du</th>
                    <th>Montant Restant du </th>
                    {# <th></th> #}
                </tr>
            </thead>
            <tbody>
                {% for tableau in tableau %}
                    <tr>
                        <td> {{ tableau.periode}} </td>
                        <td>{{ tableau.dateRemborsement|date("d/m/Y")}}</td>
                        <td>{{ tableau.principale }}</td>
                        <td>{{ tableau.interet }}</td>
                        <td>{{ tableau.montanttTotal }}</td>
                        <td>{{ tableau.soldedu }}</td>
                        <td>{{ tableau.InteretDu }}</td>
                        <td>{{ tableau.MontantRestantDu }}</td>
                        {# <td> <a href="{{ path('app_amortissement_fixe_edit',{'id': tableau.id}) }}" class="btn btn-sm btn-info"> <i class="fa fa-edit"></i> </a></td> #}
                    </tr>
                {% endfor %}
                <tr>
                    <th colspan="2">Total </th>
                    <th> {{ SommeCapital }} </th>
                    <th> {{ SommeInteret }} </th>
                    <th> {{ SommeCredit}} </th>
                </tr>
            </tbody>
        </table>
        {# <div class="d-flex justify-content-center">
            {{ form_start(form)}}
                {{ form_widget(form) }}
            {{ form_end(form)}}
        </div> #}
    </div>

{% endblock %}
