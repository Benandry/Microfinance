<form method="post" action="{{ path('app_approbation_credit_delete', {'id': approbation_credit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ approbation_credit.id) }}">
    <button class="btn">Delete</button>
</form>
