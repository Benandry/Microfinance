{{ form_start(form) }}
<div class="row">
    <div class="col-6 col-sm-6" style="display:none;">
        {{ form_row(form.NumeroCredit) }}
    </div>
    <div class="col-6 col-sm-6">
        {{ form_row(form.PasseEnPerte) }}
    </div>
</div>
    <div class="d-flex justify-content-center">
        <button class="btn btn-sm btn-primary m-2">{{ button_label|default('Enregistrer') }} </button>
    </div>
{{ form_end(form) }}