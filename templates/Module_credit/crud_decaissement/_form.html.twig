<hr/>
{{ form_start(form) }}
    <div class="row">
        <div class="col-sm-4 col-xl-4">
        {#  #}
        {% if Mode == 'DAV' %}
            <div style="display:none">
                {{ form_row(form.numeroCredit) }}
            </div>
                {{ form_row(form.dateDecaissement) }}
                {{ form_row(form.idepargne) }}
                {{ form_row(form.pieceComptable) }}
        {% else %}
            <div style="display:none">
                {{ form_row(form.numeroCredit) }}
                {{ form_row(form.idepargne) }}
            </div>
                {{ form_row(form.dateDecaissement) }}
                {{ form_row(form.pieceComptable) }}
        {% endif %}
        {#  #}
            <div class="row">
            </div>
        </div>
        <div class="col-sm-4 col-xl-4">
            {{ form_row(form.commissionCredit) }}
            {{ form_row(form.fraisDeDeveloppement) }}
        </div>
        <div class="col-sm-4 col-xl-4">
            {{ form_row(form.OrigineFond) }}
            <div style="display:none">{{ form_row(form.NumeroCompteEpargne) }}</div>
            <div style="display:none">{{ form_row(form.montantCredit) }}</div>
        </div>
    </div>

    <div class="row">
        <div class="col-sm-4 col-xl-4">
            {{ form_row(form.Filiere) }}
        </div>
        <div class="col-sm-4 col-xl-4">
            {{ form_row(form.ChaineValeur) }}
        </div>
    </div>

    <hr/>
        </div>
    </div>

    <div class="d-flex justify-content-center">
        <div class="row">
            <div class="col-sm-6 mt-2">
                <button class="btn btn-sm btn-primary">{{ button_label|default('Décaisser') }}</button>
            </div>
        </div>
    </div>
{{ form_end(form) }}
