{{ form_start(form) }}
    <ul class="nav nav-tabs">
         <li class="nav-item"> <a class="nav-link active" data-bs-toggle="tab" href="#home">Fond Credit</a></li>
      </ul>
    <div class="tab-content">  
        <div class="tab-pane active" id="home">
            <div class="row border g-0 rounded shadow-sm p-3">
               <div class="col-6 col-sm-6 col-md-6">
                    {{ form_row(form.NomBailleurs) }}
                    {{ form_row(form.Montant) }}
               </div>
                 <div class="col-6 col-sm-6">
                    {{ form_row(form.date) }}
                    {{ form_row(form.devise) }}
                    {{ form_row(form.NumeroCompte) }}
                </div>               
            </div>
         </div>
    </div>  
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
