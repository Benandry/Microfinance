<form method="post" action="{{ path('app_plan_comptable_delete', {'id': plan_comptable.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ plan_comptable.id) }}">
    <button class="btn">Delete</button>
</form>
