{{ form_start(form) }}

    <div class="rounded g-0 shadow-sm p-3">
        <div class="row">
            <div class="col-6 col-sm-6">
                {{ form_row(form.NomAgence) }}
                {{ form_row(form.AdressAgence) }}
            </div>
            <div class="col-6 col-sm-6">
                {{ form_row(form.commune) }}
                <div id="commune_suggest"></div>
            </div>
        </div>
        <div class="row">
            <div class="col-2 col-sm-2">
                {{ form_row(form.Save) }}
            </div>
            <div class="col-auto col-sm-auto col-md-auto">
                <a href="{{ path('app_agence_index') }}" class="btn btn-info btn-sm"><i class="fa fa-list"></i> Agence existe</a>
            </div>
        </div>
    </div>
{{ form_end(form) }}
