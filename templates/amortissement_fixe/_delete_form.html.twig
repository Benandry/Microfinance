<form method="post" action="{{ path('app_amortissement_fixe_delete', {'id': amortissement_fixe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ amortissement_fixe.id) }}">
    <button class="btn">Delete</button>
</form>
