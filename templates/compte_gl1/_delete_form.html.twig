<form method="post" action="{{ path('app_compte_g_l1_delete', {'id': compte_g_l1.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ compte_g_l1.id) }}">
    <button class="btn">Delete</button>
</form>
