{% extends 'base.html.twig' %}

{% block title %}CompteGL1{% endblock %}

{% block body %}
    <h1>CompteGL1</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ compte_g_l1.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_compte_g_l1_index') }}">back to list</a>

    <a href="{{ path('app_compte_g_l1_edit', {'id': compte_g_l1.id}) }}">edit</a>

    {{ include('compte_gl1/_delete_form.html.twig') }}
{% endblock %}
