{% extends 'base.html.twig' %}

{% block title %}CompteGL1 index{% endblock %}

{% block body %}
    <h1>CompteGL1 index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for compte_g_l1 in compte_g_l1s %}
            <tr>
                <td>{{ compte_g_l1.id }}</td>
                <td>
                    <a href="{{ path('app_compte_g_l1_show', {'id': compte_g_l1.id}) }}">show</a>
                    <a href="{{ path('app_compte_g_l1_edit', {'id': compte_g_l1.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_compte_g_l1_new') }}">Create new</a>
{% endblock %}
