<form method="post" action="{{ path('app_compte_caisse_delete', {'id': compte_caisse.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ compte_caisse.id) }}">
    <button class="btn btn-danger btn-sm" >Supprimer</button>
</form>
