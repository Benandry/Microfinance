{% extends "base.html.twig" %}

{% block title %}
    Remboursement 
{% endblock %}

{% block head %}
    Remboursement 
{% endblock %}

{% block body %}

    {# /***************Messages succes ******************/#}
    
            {{ include('modal.html.twig') }}

    {# *************************************#}

    <div class="card p-3">
        <div class="card mb-3">
            <div class="row text-secondary m-3">
                <div class="col-sm-4 col-xl-4">
                    <p><b>Code Credit : </b><span id="codecreditremboursement">{{ numerocredit }}</span></p>
                    <p> <b>reste  : </b><span id="reste">{{ restemontant }}</span></p>
                    {# <p> <b>Total periode  : </b><span id="total periode">{{ totalperiode }}</span></p> #}
                    <p> <b>Montant du  : </b><span id="total periode">{{ montantdu }}</span></p>
                </div>
                 <div class="col-sm-4 col-xl-4">
                    <p> <b>periode precedent  : </b><span id="periodeprecedent">{{ periode }}</span></p>
                    <p> <b>periode  : </b><span id="periode"></span></p>
                    <p> <b>Total credit  : </b><span id="crd">{{ crd }}</span></p>
                </div>
                 <div class="col-sm-4 col-xl-4">
                    <p> <b>Credit deja rembourser  : </b><span id="crd">{{ TotalRembourser }}</span></p>
                    <p> <b>Credit pas encore rembourser  : </b><span id="crd">
                    {% if TotalaRembourser != null %}
                    {{ TotalaRembourser }}
                    {% else %}
                    {{ crd }}
                    {% endif %}
                    </span></p>
                     <p><b>Mode de remboursement:</b><span>{{ Mode }}</span></p>
                </div>
            </div>
        </div>
    </div>

    <div class="col-xl-3 col-md-3 col-sm-3">
    </div>
</div>

    {{ include('remboursement_credit/_form.html.twig') }}

{% endblock %}
