
{{ form_start(form) }}
    <div class="row" >
        <div class="col-sm-12" style="display:none;">
            {{ form_row(form.typeClient) }}
            {{ form_row(form.codeepargneclient) }}
            <span id="grp">{{ form_row(form.nomgroupe) }}</span>
            {{ form_row(form.Description) }}
            {{ form_row(form.Montant) }}
            {{ form_row(form.devise) }}
            </div>
            {# ********************fin ***************************** #}
        </div>
    </div>
<hr>
    <div class="row">
        <div class="col-sm-4 col-md-4 m-auto">
            {{ form_row(form.DateTransaction) }}
        </div>
        <div class="col-sm-4 col-md-4 m-auto">
            {{ form_row(form.PieceComptable) }}
        </div>
    </div>
    <div class="row">
        <div class="col-sm-4 col-md-4 m-auto">
            {{ form_row(form.montant_bruite) }}
        </div>
    </div>
<hr/>
    <div style="display: none;">
        {{ form_row(form.solde) }}
    </div>
    <div class="d-flex justify-content-center">
        <button class="btn btn-primary">{{ button_label|default('Valider le dépôt') }}</button>
    </div>
{{ form_end(form) }}
