
{{ form_start(form) }}
    <div class="row" >
        <div class="col-sm-12" style="display:none;">
            {{ form_row(form.typeClient) }}
            {{ form_row(form.codeepargneclient) }}
            {{ form_row(form.Montant) }}
            {{ form_row(form.devise) }}
            </div>
        </div>
    </div>
<hr/>
    <div class="row">
        <div class="col-sm-4 col-md-4 m-auto">
            {{ form_row(form.DateTransaction) }}
        </div>
        <div class="col-sm-4 col-md-4 m-auto">
            {{ form_row(form.PieceComptable) }}
        </div>
    </div>
    <div class="row">
        <div class="col-sm-4 col-md-4 m-auto">
            {{ form_row(form.montant_bruite) }}
        </div>
        <div class="col-sm-4 col-md-4 m-auto">
            {{ form_row(form.compteCaisse) }}
        </div>
    </div>
<hr/>
    <div style="display: none;">
        {{ form_row(form.solde) }}
    </div>
    <div class="d-flex justify-content-center">
        <button class="btn btn-primary"><i class="fas fa-sign-in"></i> {{ button_label|default('Valider le Entrée ') }}</button>
    </div>
{{ form_end(form) }}
