<form method="post" action="{{ path('app_mouvement_comptable_delete', {'id': mouvement_comptable.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mouvement_comptable.id) }}">
    <button class="btn">Delete</button>
</form>
