<form method="post" action="{{ path('app_garantie_credit_delete', {'id': garantie_credit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ garantie_credit.id) }}">
    <button class="btn">Delete</button>
</form>
